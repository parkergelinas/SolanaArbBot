[package]
name = "graph"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
common = { path = "../common" }
decoder = { version = "0.1.0", path = "../decoder" }
