Control API
-----------

HTTP API for controlling runtime configuration and querying state.

Run locally:

```bash
cargo run -p control-api
```

Check `apps/control-api` for further runtime env variables and configuration.
