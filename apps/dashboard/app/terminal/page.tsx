'use client';

import PanelLayout from '@/components/layout/PanelLayout';

/**
 * Professional trading terminal — Bloomberg × Linear layout.
 * Data: stream-api WS + paper portfolio; dry-run by default.
 */
export default function TerminalPage() {
  return <PanelLayout />;
}
