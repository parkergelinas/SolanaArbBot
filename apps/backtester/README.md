Backtester
----------

Rust backtesting service used to run historical simulations against price data.

Build and run (from repo root):

```bash
cargo run -p backtester --release
```

See `crates/backtester` for library components used by the runner.
