/** Risk layer — per-strategy limits, cooldowns, inventory caps. */
export { checkStrategyRisk, DEFAULT_RISK_LIMITS, recordExecutionOutcome, type RiskCheckResult, type RiskVerdict, type StrategyRiskLimits, type StrategyRiskState, } from './limits.js';
