Data
----

Contains datasets and backtest results used by the repository.

Files:
- `backtest_results.json` — example/backtest output used by the dashboard and tests

Large datasets are not stored in the repository. Use external storage or local caching for full market history.
