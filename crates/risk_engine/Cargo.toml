[package]
name = "risk_engine"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
# Canonical implementation lives in portfolio.
# risk_engine is a backward-compatibility re-export layer.
portfolio = { path = "../portfolio" }
