[package]
name = "rpc_client"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
# Canonical implementation lives in ingestion::rpc.
# rpc_client is a backward-compatibility re-export layer.
ingestion = { path = "../ingestion" }
