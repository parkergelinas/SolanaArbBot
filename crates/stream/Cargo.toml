[package]
name = "stream"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
# Canonical implementations live in events and ingestion.
# stream is a backward-compatibility re-export layer.
common = { path = "../common" }
events = { path = "../events" }
ingestion = { path = "../ingestion" }

[dev-dependencies]
tokio = { version = "1.52.3", features = ["full"] }
tracing = "0.1.44"
