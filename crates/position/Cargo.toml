[package]
name = "position"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
# Canonical implementation lives in portfolio.
# position is a backward-compatibility re-export layer.
portfolio = { path = "../portfolio" }
