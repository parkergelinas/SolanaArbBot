[package]
name = "accounts"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
# Canonical implementation lives in storage.
# accounts is a backward-compatibility re-export layer.
storage = { path = "../storage" }
