Frontend
--------

Front-end applications and UI components. This repository contains two Next.js apps: `frontend/` and `dashboard/`.

Quick commands (from workspace root):

```bash
cd frontend
pnpm install
pnpm dev   # local development
pnpm build # production build
```

See each app's `package.json` for scripts and dependencies.
